extends layout.pug

block content
  article.grid-container.portfolio-intro
    div
      h1 Angela's Portfolio
      p.lead.text-light
        | Hello. Following is a list of my most recent projects. I am currently looking for front end engineer job. Please feel free to contact me for any potential opportunities.
  article.grid-container.portfolio-index
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
        //- iterate over and inject data that passed in
        each val, index in data
          .cell
            a(href='/project/' + data[index]['id'])
              img.thumbnail(src='/static/' + data[index]['image_urls'][0] + '.png')
              h5 #{data[index]['project_name']}
